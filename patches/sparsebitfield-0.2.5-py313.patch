--- a/cimpl/field.pyx	2018-07-27 13:50:56.000000000 +0200
+++ b/cimpl/field.pyx
@@ -624,7 +624,7 @@
         if isinstance(data, bytes):
             return self.load_from_bytes(data)
         for item in data:
-            if isinstance(item, (int, long)):
+            if isinstance(item, int):
                 self.add(item)
             else:
                 low, high = item
